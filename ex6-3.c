#include <stdio.h>

int main()
{
    int count = 0;  // 변수 선언(생성)

    //count = 10;     // 변수 사용.

    for ( count = 0; count < 10; ++count )
    //for ( ; 1 ;  )  // while(1)
    {
        printf("hello\n");
    }

    return 0;

}































//
