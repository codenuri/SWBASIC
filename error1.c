#include <stdio.h>

int main()
{
    int a = 0, b = 0;

    scanf("%d", &a);
    scanf("%d", &b);

    printf("+ : %d\n", a + b);
    printf("- : %d\n", a - b);
    printf("* : %d\n", a * b);

    if ( b != 0)
        printf("/ : %d\n", a / b);

    //int x[2];
    //x[5] = 0;

    return 0;
}






















//
